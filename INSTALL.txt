UFN CAMPAIGN PLAYER FIX + CUSTOM LINKS
======================================

1. In GitHub repo DJSmartyP/UFN-crew-tool replace:
   - campaign.js
   - firestore.rules

2. Wait for GitHub Pages to deploy.

3. In Firebase project bc-crew-autoassigner:
   Firestore Database > Rules
   Replace the rules with the COMPLETE firestore.rules supplied here and Publish.
   This is still the combined rules file for the main IDP, UFN, and campaign crews.

4. Test an existing ?m=... player link in an incognito window and register choices.

5. New campaign deployments now include a 'Custom player link name' field.
   Example: celeste-continuum -> ?m=celeste-continuum
   Link names are checked for uniqueness across the planner and are fixed after creation.

Existing deployment links continue to work unchanged.
